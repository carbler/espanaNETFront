/**
 * Created by EspañaNet on 28/11/2016.
 */

/**(function () {
    'use strict';
    angular
        .module('app.login')
        .config('jwt', config);
    /** @ngInject */
/*function config($httpProvider, JwtOptionsProvider) {
 JwtOptionsProvider.config({
 getToken: getToken
 });
 /** @ngInject */
/* function getToken(options, jwtHelper, LoginService) {
 var jwt = localStorage.getItem('jwt');
 if(jwt && options.url.indexOf(user._getUrl()) === 0){
 if(jwtHelper.isTokenExpiderd(jwt)){
 //aca pone el metodo para refrestcar el token
 //1. return jwt
 //2. return null
 }
 }
 }
 }
 })();
 */